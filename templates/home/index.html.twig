<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AeroManager - Système de Gestion Aérienne</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            overflow-x: hidden;
        }

        /* Navigation */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            box-shadow: 0 2px 20px rgba(0, 0, 0, 0.1);
            z-index: 1000;
            padding: 1rem 0;
        }

        .nav-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: 700;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .nav-links {
            display: flex;
            gap: 2rem;
            align-items: center;
        }

        .nav-links a {
            text-decoration: none;
            color: #333;
            font-weight: 500;
            transition: color 0.3s;
        }

        .nav-links a:hover {
            color: #667eea;
        }

        .btn {
            padding: 0.7rem 1.5rem;
            border-radius: 25px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s;
            border: none;
            cursor: pointer;
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 20px rgba(102, 126, 234, 0.4);
        }

        .btn-outline {
            border: 2px solid #667eea;
            color: #667eea;
            background: transparent;
        }

        .btn-outline:hover {
            background: #667eea;
            color: white;
        }

        /* Hero Section */
        .hero {
            margin-top: 80px;
            padding: 4rem 2rem;
            text-align: center;
            color: white;
        }

        .hero h1 {
            font-size: 3.5rem;
            margin-bottom: 1rem;
            animation: fadeInUp 0.8s ease-out;
        }

        .hero p {
            font-size: 1.3rem;
            margin-bottom: 2rem;
            opacity: 0.95;
            animation: fadeInUp 0.8s ease-out 0.2s both;
        }

        .hero-buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            animation: fadeInUp 0.8s ease-out 0.4s both;
        }

        /* Stats Section */
        .stats {
            max-width: 1400px;
            margin: 3rem auto;
            padding: 0 2rem;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 2rem;
        }

        .stat-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            padding: 2rem;
            border-radius: 20px;
            text-align: center;
            color: white;
            border: 1px solid rgba(255, 255, 255, 0.2);
            transition: transform 0.3s;
        }

        .stat-card:hover {
            transform: translateY(-5px);
        }

        .stat-number {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        .stat-label {
            font-size: 1rem;
            opacity: 0.9;
        }

        /* Main Content */
        .main-content {
            background: white;
            border-radius: 30px 30px 0 0;
            margin-top: 3rem;
            padding: 4rem 2rem;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
        }

        .section-title {
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 1rem;
            color: #333;
        }

        .section-subtitle {
            text-align: center;
            color: #666;
            margin-bottom: 3rem;
            font-size: 1.1rem;
        }

        /* Features Grid */
        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin-bottom: 4rem;
        }

        .feature-card {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            padding: 2rem;
            border-radius: 20px;
            transition: all 0.3s;
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            opacity: 0;
            transition: opacity 0.3s;
        }

        .feature-card:hover::before {
            opacity: 0.9;
        }

        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
        }

        .feature-card:hover .feature-content {
            color: white;
        }

        .feature-content {
            position: relative;
            z-index: 1;
            transition: color 0.3s;
        }

        .feature-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        .feature-title {
            font-size: 1.5rem;
            margin-bottom: 0.5rem;
            font-weight: 600;
        }

        .feature-description {
            opacity: 0.8;
            line-height: 1.6;
        }

        /* Dashboard Preview */
        .dashboard-preview {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 4rem 2rem;
            border-radius: 30px;
            margin: 4rem 0;
            color: white;
        }

        .dashboard-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-top: 2rem;
        }

        .dashboard-module {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(10px);
            padding: 2rem;
            border-radius: 15px;
            border: 1px solid rgba(255, 255, 255, 0.3);
            transition: all 0.3s;
        }

        .dashboard-module:hover {
            background: rgba(255, 255, 255, 0.25);
            transform: scale(1.05);
        }

        .module-icon {
            font-size: 2.5rem;
            margin-bottom: 1rem;
        }

        .module-title {
            font-size: 1.3rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }

        .module-count {
            font-size: 2rem;
            font-weight: 700;
            opacity: 0.9;
        }

        /* CTA Section */
        .cta-section {
            text-align: center;
            padding: 4rem 2rem;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            border-radius: 30px;
            margin: 4rem 0;
        }

        .cta-section h2 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            color: #333;
        }

        .cta-section p {
            font-size: 1.2rem;
            color: #666;
            margin-bottom: 2rem;
        }

        /* Footer */
        footer {
            background: #1a1a2e;
            color: white;
            padding: 3rem 2rem;
            text-align: center;
        }

        .footer-content {
            max-width: 1400px;
            margin: 0 auto;
        }

        /* Animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .hero h1 {
                font-size: 2.5rem;
            }

            .nav-links {
                gap: 1rem;
            }

            .features-grid {
                grid-template-columns: 1fr;
            }

            .hero-buttons {
                flex-direction: column;
                align-items: center;
            }
        }
    </style>
</head>
<body>
<!-- Navigation -->
<nav>
    <div class="nav-container">
        <div class="logo">
            ✈️ AeroManager
        </div>
        <div class="nav-links">
            <a href="#features">Fonctionnalités</a>
            <a href="#modules">Modules</a>
            <a href="#contact">Contact</a>
            <a href="/login" class="btn btn-outline">Connexion</a>
            <a href="/register" class="btn btn-primary">S'inscrire</a>
        </div>
    </div>
</nav>

<!-- Hero Section -->
<section class="hero">
    <h1>Gestion Aérienne Intelligente</h1>
    <p>Plateforme complète pour orchestrer vos opérations aériennes en temps réel</p>
    <div class="hero-buttons">
        <a href="/register" class="btn btn-primary">Démarrer Gratuitement</a>
        <a href="#features" class="btn btn-outline">Découvrir Plus</a>
    </div>
</section>

<!-- Stats Section -->
<section class="stats">
    <div class="stat-card">
        <div class="stat-number">500+</div>
        <div class="stat-label">Vols Gérés</div>
    </div>
    <div class="stat-card">
        <div class="stat-number">150+</div>
        <div class="stat-label">Avions Enregistrés</div>
    </div>
    <div class="stat-card">
        <div class="stat-number">50K+</div>
        <div class="stat-label">Passagers</div>
    </div>
    <div class="stat-card">
        <div class="stat-number">99.9%</div>
        <div class="stat-label">Disponibilité</div>
    </div>
</section>

<!-- Main Content -->
<div class="main-content">
    <div class="container">
        <!-- Features Section -->
        <section id="features">
            <h2 class="section-title">Fonctionnalités Principales</h2>
            <p class="section-subtitle">Tout ce dont vous avez besoin pour gérer votre flotte aérienne</p>

            <div class="features-grid">
                <div class="feature-card">
                    <div class="feature-content">
                        <div class="feature-icon">👥</div>
                        <h3 class="feature-title">Gestion Utilisateurs</h3>
                        <p class="feature-description">Administrez vos équipes avec des rôles personnalisés : administrateurs, clients et passagers avec permissions granulaires.</p>
                    </div>
                </div>

                <div class="feature-card">
                    <div class="feature-content">
                        <div class="feature-icon">✈️</div>
                        <h3 class="feature-title">Flotte d'Avions</h3>
                        <p class="feature-description">Enregistrez, modifiez et suivez l'état de votre flotte en temps réel avec maintenance et historique complet.</p>
                    </div>
                </div>

                <div class="feature-card">
                    <div class="feature-content">
                        <div class="feature-icon">🏢</div>
                        <h3 class="feature-title">Réseau d'Aéroports</h3>
                        <p class="feature-description">Gérez tous les aéroports desservis, leurs capacités et connexions pour optimiser vos itinéraires.</p>
                    </div>
                </div>

                <div class="feature-card">
                    <div class="feature-content">
                        <div class="feature-icon">🛫</div>
                        <h3 class="feature-title">Planification des Vols</h3>
                        <p class="feature-description">Créez et gérez vos vols avec horaires, routes, équipages et suivi en temps réel des statuts.</p>
                    </div>
                </div>

                <div class="feature-card">
                    <div class="feature-content">
                        <div class="feature-icon">📋</div>
                        <h3 class="feature-title">Réservations Avancées</h3>
                        <p class="feature-description">Système de réservation complet avec gestion des sièges, modifications et annulations simplifiées.</p>
                    </div>
                </div>

                <div class="feature-card">
                    <div class="feature-content">
                        <div class="feature-icon">🎫</div>
                        <h3 class="feature-title">Tickets Numériques</h3>
                        <p class="feature-description">Génération automatique de tickets électroniques avec QR codes et enregistrement mobile.</p>
                    </div>
                </div>

                <div class="feature-card">
                    <div class="feature-content">
                        <div class="feature-icon">💳</div>
                        <h3 class="feature-title">Paiements Sécurisés</h3>
                        <p class="feature-description">Processus de paiement crypté avec multiples méthodes et gestion des remboursements automatisée.</p>
                    </div>
                </div>

                <div class="feature-card">
                    <div class="feature-content">
                        <div class="feature-icon">🏷️</div>
                        <h3 class="feature-title">Catégories d'Avions</h3>
                        <p class="feature-description">Classifiez votre flotte par type, capacité et classe de service pour une gestion optimisée.</p>
                    </div>
                </div>

                <div class="feature-card">
                    <div class="feature-content">
                        <div class="feature-icon">👤</div>
                        <h3 class="feature-title">Profils Passagers</h3>
                        <p class="feature-description">Base de données complète avec historique de voyages, préférences et programmes de fidélité.</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- Dashboard Preview -->
        <section id="modules" class="dashboard-preview">
            <h2 class="section-title">Tableau de Bord Centralisé</h2>
            <p class="section-subtitle">Visualisez et contrôlez toutes vos opérations depuis une interface unique</p>

            <div class="dashboard-grid">
                <div class="dashboard-module">
                    <div class="module-icon">👨‍💼</div>
                    <div class="module-title">Administrateurs</div>
                    <div class="module-count">12 actifs</div>
                </div>

                <div class="dashboard-module">
                    <div class="module-icon">👥</div>
                    <div class="module-title">Clients</div>
                    <div class="module-count">2,547</div>
                </div>

                <div class="dashboard-module">
                    <div class="module-icon">✈️</div>
                    <div class="module-title">Avions</div>
                    <div class="module-count">156</div>
                </div>

                <div class="dashboard-module">
                    <div class="module-icon">🏢</div>
                    <div class="module-title">Aéroports</div>
                    <div class="module-count">48</div>
                </div>

                <div class="dashboard-module">
                    <div class="module-icon">🛫</div>
                    <div class="module-title">Vols Aujourd'hui</div>
                    <div class="module-count">87</div>
                </div>

                <div class="dashboard-module">
                    <div class="module-icon">📋</div>
                    <div class="module-title">Réservations</div>
                    <div class="module-count">1,234</div>
                </div>

                <div class="dashboard-module">
                    <div class="module-icon">🎫</div>
                    <div class="module-title">Tickets Émis</div>
                    <div class="module-count">3,891</div>
                </div>

                <div class="dashboard-module">
                    <div class="module-icon">💰</div>
                    <div class="module-title">Revenus Mois</div>
                    <div class="module-count">$547K</div>
                </div>
            </div>
        </section>

        <!-- CTA Section -->
        <section class="cta-section">
            <h2>Prêt à Décoller ?</h2>
            <p>Rejoignez des centaines d'organisations qui font confiance à AeroManager</p>
            <div class="hero-buttons">
                <a href="/register" class="btn btn-primary">Commencer Maintenant</a>
                <a href="/demo" class="btn btn-outline">Demander une Démo</a>
            </div>
        </section>
    </div>
</div>

<!-- Footer -->
<footer>
    <div class="footer-content">
        <p>&copy; 2024 AeroManager. Tous droits réservés.</p>
        <p>Plateforme de Gestion Aérienne Professionnelle</p>
    </div>
</footer>

<script>
    // Smooth scrolling for anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            e.preventDefault();
            const target = document.querySelector(this.getAttribute('href'));
            if (target) {
                target.scrollIntoView({
                    behavior: 'smooth',
                    block: 'start'
                });
            }
        });
    });

    // Add animation on scroll
    const observerOptions = {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.style.opacity = '1';
                entry.target.style.transform = 'translateY(0)';
            }
        });
    }, observerOptions);

    document.querySelectorAll('.feature-card, .dashboard-module').forEach(el => {
        el.style.opacity = '0';
        el.style.transform = 'translateY(30px)';
        el.style.transition = 'all 0.6s ease-out';
        observer.observe(el);
    });
</script>
</body>
</html>
